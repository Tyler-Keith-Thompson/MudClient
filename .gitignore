.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Bazel
/bazel-*
MODULE.bazel.lock
user.bazelrc
/swift/.build

# AI pilot runtime artifacts
Scripts/.ai-script-requests.jsonl
ai-traces.jsonl

# raw capture (debug, ring-buffered)
mud_raw.log

# Finetune model checkpoints — training outputs, multi-GB. Keep adapter_config.json, ignore weights.
*.safetensors
